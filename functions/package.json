{
  "name": "dcse-functions",
  "description": "Firebase Cloud Functions for DCSE (e.g. assignment notifications)",
  "main": "index.js",
  "engines": { "node": "18" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.5.0"
  }
}
